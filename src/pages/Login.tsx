import { useTranslation } from "react-i18next";

const Login = () => {
  const { t, i18n } = useTranslation();
  return <div className="text-primary font-semibold text-4xl">Login</div>;
};

export default Login;
